# OpenAI API Configuration
# Required for: Whisper STT (cross-browser), Query rephrasing/cleanup
OPENAI_API_KEY=your-api-key

# MiniMax API Configuration (Server-side only - SECURE)
# Required for: High-quality TTS streaming with custom voice cloning
# Get your API key and Group ID from: https://platform.minimax.io/
# API key is kept secret on the server, never exposed to the browser
MINIMAX_API_KEY=your-api-key
MINIMAX_GROUP_ID=your-api-key

# MiniMax Voice Configuration
# Using default voice (available to all users)
# Options: 'female-shaonv' (young female), 'male-qn-qingse' (male), 'female-yujie' (mature female), 
#          'male-qn-jingying' (business male), 'Audiobook_male_1', 'Audiobook_female_1'
MINIMAX_VOICE_ID=female-shaonv

# ElevenLabs API Configuration
# Required for: WebSocket TTS streaming (client-side) and REST API (server-side)
ELEVENLABS_API_KEY=your-api-key
ELEVENLABS_VOICE_ID=EST9Ui6982FZPSi7gCHi

# AIP ActionCable Configuration (client-side)
# Example: ws://localhost:3001/cable
NEXT_PUBLIC_AIP_CABLE_URL=ws://localhost:3001/cable
NEXT_PUBLIC_AIP_MODEL_ID=develop_org_7_Gina_Test2